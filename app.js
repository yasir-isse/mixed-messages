const word1 = ['Always', 'I can', 'I\'ll'];
const word2 = ['be', 'become', 'act'];
const word3 = ['better.', 'strong', 'brave'];

let random = Math.floor(Math.random() * 3);

console.log(`${word1[random]} ${word2[random]} ${word3[random]}`);
